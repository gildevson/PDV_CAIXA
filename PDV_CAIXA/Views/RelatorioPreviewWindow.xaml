<Window x:Class="PDV_CAIXA.Views.RelatorioPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Visualizar Relatório"
        WindowStartupLocation="CenterScreen"
        Width="900" Height="700"
        MinWidth="600" MinHeight="400"
        Background="#0A0A16">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Border Grid.Row="0" Padding="16,10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#080812" Offset="0"/>
                    <GradientStop Color="#0E0E1C" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📄" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="VISUALIZADOR DE RELATÓRIO" Foreground="#44446A"
                                   FontSize="9" FontWeight="Bold"/>
                        <TextBlock x:Name="txtTituloRelatorio" Text=""
                                   Foreground="#D0D0E0" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="btnImprimir" Content="🖨 Imprimir"
                            Height="30" Padding="14,0" Margin="0,0,8,0"
                            FontSize="12" FontWeight="SemiBold"
                            Background="#0C2014" Foreground="#50FA7B"
                            BorderThickness="1" BorderBrush="#1A4A2A"
                            Cursor="Hand" Click="BtnImprimir_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}"
                                        CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#142A1C"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#2A6A3A"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="✕ Fechar"
                            Height="30" Padding="14,0"
                            FontSize="12" FontWeight="SemiBold"
                            Background="#1A0808" Foreground="#6A3030"
                            BorderThickness="1" BorderBrush="#2A1010"
                            Cursor="Hand" Click="BtnFechar_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}"
                                        CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#2A0C0C"/>
                                        <Setter TargetName="bd" Property="BorderBrush" Value="#4A1818"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        <Rectangle Grid.Row="0" Height="1" VerticalAlignment="Bottom" Fill="#14142A"/>

        <!-- WebView2 — exibe o PDF diretamente aqui dentro -->
        <wv2:WebView2 Grid.Row="1" x:Name="webView" />
    </Grid>
</Window>
